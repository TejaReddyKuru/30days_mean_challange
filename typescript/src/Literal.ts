function pizzaMenu(size:"small" | "large" | "Mediam"){
    if(size === "large"){
        console.log(size);
        return;
    }

    if(size === "small"){
        console.log(size);
        return;
    }

}